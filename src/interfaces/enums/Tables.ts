enum TABLES {
    CUSTOMERS = 'clientes',
    CARD_BALANCE = 'card_balance',
    CARDS = 'cards'
}

enum COLUMNS{
    DOCUMENT = 'nro_documento',
    ID_CUSTOM = 'id_custom',
    PASSWORD = 'password',
}

export {
    TABLES,
    COLUMNS
}
